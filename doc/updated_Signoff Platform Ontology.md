# Signoff Ontology 구조 설계서

---

## 목차

### Part 1: 개념 및 구조

1. [개요](#1-%EA%B0%9C%EC%9A%94)
2. [Ontology 기초 개념](#2-ontology-%EA%B8%B0%EC%B4%88-%EA%B0%9C%EB%85%90)
3. [Signoff Ontology 3-Layer 구조](#3-signoff-ontology-3-layer-%EA%B5%AC%EC%A1%B0)
4. [Phase별 구축 로드맵](#4-phase%EB%B3%84-%EA%B5%AC%EC%B6%95-%EB%A1%9C%EB%93%9C%EB%A7%B5)

### Part 2: 구현 상세

4. [Object Type 상세 정의](https://claude.ai/chat/d585c994-75a9-4aea-99f1-4ec0bd7a1945#4-object-type-%EC%83%81%EC%84%B8-%EC%A0%95%EC%9D%98)
5. [관계(Links) 전체 정의](https://claude.ai/chat/d585c994-75a9-4aea-99f1-4ec0bd7a1945#5-%EA%B4%80%EA%B3%84links-%EC%A0%84%EC%B2%B4-%EC%A0%95%EC%9D%98)
6. [Signoff Workflow와 Ontology 매핑](https://claude.ai/chat/d585c994-75a9-4aea-99f1-4ec0bd7a1945#6-signoff-workflow%EC%99%80-ontology-%EB%A7%A4%ED%95%91)
7. [예시 시나리오](https://claude.ai/chat/d585c994-75a9-4aea-99f1-4ec0bd7a1945#7-%EC%98%88%EC%8B%9C-%EC%8B%9C%EB%82%98%EB%A6%AC%EC%98%A4)
8. [향후 확장 계획](https://claude.ai/chat/d585c994-75a9-4aea-99f1-4ec0bd7a1945#8-%ED%96%A5%ED%9B%84-%ED%99%95%EC%9E%A5-%EA%B3%84%ED%9A%8D)

---

# Part 1: 개념 및 구조

---

## 1. 개요

### 1.1 이 문서의 목적

본 문서는 차세대 Signoff Platform의 핵심 기반인 **Signoff Ontology**의 구조를 정의한다.

**Signoff Ontology**란 메모리 회로 설계 검증(Signoff) 업무의 핵심 개체(Object)들과 그들 간의 관계(Relationship)를 체계적으로 정의한 **지식 표현 체계**이다. 기존의 File/Folder/DB 기반의 파편화된 데이터 관리 방식에서 벗어나, 데이터의 **"의미(Meaning)"**와 **"맥락(Context)"**을 시스템이 이해할 수 있는 Knowledge Graph 형태로 전환하는 것을 목표로 한다.

이를 통해 **AI Agent가 Signoff 업무를 스스로 수행하고 추론할 수 있는 기반**을 마련하고자 한다.

### 1.2 왜 Ontology가 필요한가?

#### 1.2.1 현재 Signoff Platform의 구조적 한계

|문제|현재 상태|Ontology 도입 후|
|---|---|---|
|**Data Silo**|SOL/SORV 간 데이터 단절, Input↔Output 연결 부재|모든 데이터가 관계로 연결|
|**Operation Silo**|Application별 개별 point solution|통합된 공통 언어로 표준화|
|**Decision Silo**|Waiver 근거, 해결 이력 미기록|의사결정 이력 축적 및 재활용|

```
┌─────────────────────────────────────────────────────────────────────┐
│  현재 시스템: "데이터"만 관리                                         │
│  ─────────────────────────────────────────────────────────────────  │
│  • result.csv 파일이 존재함                                          │
│  • 누가, 어떤 조건으로, 왜 이 결과가 나왔는지 알 수 없음               │
│  • 이전 Revision과 비교하려면 수동 작업 필요                          │
├─────────────────────────────────────────────────────────────────────┤
│  Ontology 기반 시스템: "데이터 + 의미 + 관계" 통합 관리               │
│  ─────────────────────────────────────────────────────────────────  │
│  • Result가 어떤 Product/Revision/Block의 것인지 즉시 파악            │
│  • 어떤 InputConfig로 누가 실행했는지 추적 가능                       │
│  • 이전 Revision 결과와 자동 비교, Waiver 이관 가능                   │
│  • AI Agent가 맥락을 이해하고 자동 판단 가능                          │
└─────────────────────────────────────────────────────────────────────┘
```

> **핵심 인사이트**: 현재 시스템은 "데이터"를 관리하지만 "의미"를 관리하지 않는다. 이로 인해 완전한 자동화는 구조적으로 불가능하며, Ontology는 이 한계를 극복하기 위한 **필수 기반 인프라**이다.

### 1.3 Object Type 요약 (Phase 1: 9개)

| Layer        | Object Type                                            | 역할              | 개수  |
| ------------ | ------------------------------------------------------ | --------------- | --- |
| **Semantic** | Product, Revision, Block, Designer, SignoffApplication | 정적 마스터 데이터 (자산) | 5개  |
| **Kinetic**  | SignoffJob, InputConfig, Workspace                     | 실행 및 상태 추적 (행위) | 3개  |
| **Dynamic**  | Result                                                 | 결과 및 분석 대상 (지능) | 1개  |
## 2. Ontology 기초 개념

### 2.1 Ontology란?

**Ontology**는 특정 도메인의 **개념(Concept)**과 **관계(Relationship)**를 명시적으로 정의하여, 사람과 기계(AI)가 동일한 이해를 공유할 수 있게 하는 **지식 표현 방법**이다.

```
┌─────────────────────────────────────────────────────────────────────┐
│        일반 데이터베이스          vs           Ontology              │
├─────────────────────────────────────────────────────────────────────┤
│  • 테이블과 행/열                  • Object와 Property/Link         │
│  • 데이터만 저장                   • 데이터 + 의미 + 관계 저장       │
│  • JOIN으로 관계 조회              • 관계가 명시적으로 정의됨        │
│  • 스키마 변경 어려움              • 유연한 확장 가능                │
│  • "What" 만 저장                  • "What + Why + How" 저장        │
└─────────────────────────────────────────────────────────────────────┘
```


개발자 관점에서 "기존 RDB(Relational Database)나 단순 대시보드로도 가능한 것 아닌가?"라는 의문이 들 수 있다. Signoff 도메인에서 Ontology가 필수적인 이유는 다음과 같다.

#### 2.1.1 Signoff 도메인의 특수성: "맥락(Context)의 연결"

**기존 DB의 한계:**

- RDB는 미리 정의된 스키마와 Join 연산에 의존한다.
- **문제점**: "왜 이 Block의 LSC 결과가 Fail인가?"라는 질문에 대해, RDB는 결과값만 보여줄 뿐, **"누가, 어떤 Netlist 버전으로, 어떤 Power 설정(InputConfig)을 써서, 어떤 Workspace에서 돌렸는지"**에 대한 복합적인 인과관계를 동적으로 추적하기 어렵다.

**Ontology의 강점 (Data Lineage & Graph Traversal):**

- Signoff의 모든 요소(사람, 파일, 설정, 결과)를 **객체(Object)**로 정의하고 이들을 **관계(Link)**로 연결한다.
- LLM/AI Agent는 이 연결망을 통해 "A 설계자가 변경한 Netlist 때문에 B Block의 DSC 결과가 악화되었다"는 식의 **추론(Reasoning)**이 가능해진다.

#### 2.1.2 유연한 확장성 (Flexible Schema)

- Signoff Tool은 수시로 변경된다 (파라미터 추가, 새로운 Check 항목 등).
- RDB는 컬럼 변경 비용이 크지만, Ontology는 속성(Property)이나 객체 타입을 유연하게 추가/확장할 수 있어 **Agile한 개발**에 적합하다.

#### 2.1.3 AI Agent의 "공용어(Common Language)"

- Signoff Launcher, ResultViewer, Inhouse Tool 등 서로 다른 언어(C++, Python, Web)로 된 시스템들이 소통하기 위한 **표준 프로토콜** 역할을 수행한다.
- AI Agent는 이 Ontology를 **지도(Map)** 삼아 데이터를 탐색한다.




### 2.2 핵심 구성 요소

| 구성 요소                 | 설명                   | Signoff 예시                        |
| --------------------- | -------------------- | --------------------------------- |
| **Object Type**       | 개체의 유형(클래스) 정의       | Product, SignoffJob, Result       |
| **Object (Instance)** | Object Type의 실제 인스턴스 | "HBM4E", "JOB-20250119-001"       |
| **Property**          | Object의 속성           | product_name, job_status          |
| **Link**              | Object 간의 관계         | Product → has_revision → Revision |

### 2.3 왜 Knowledge Graph인가?

Ontology를 **Knowledge Graph** 형태로 구현하면 다음이 가능해진다:

**1. 관계 기반 탐색**

- "HBM4E R30의 모든 DSC 결과"를 관계를 따라 즉시 조회

**2. 맥락 이해**

- AI가 "이 Result가 어떤 제품의, 어떤 Revision의, 누가 수행한 것인지" 파악

**3. Multi-hop 추론**

- "R20에서 Waiver한 항목 중 R30에서 재발생한 것" 같은 복합 질의 가능

```
[HBM4E] ──has_revision──▶ [R30] ──has_block──▶ [FULLCHIP]
   │                        │                       │
   │                        │                       └──responsible_designer──▶ [최원우]
   │                        │
   │                        └──◀── for_revision ── [InputConfig-001]
   │                                                      │
   │                                                      └──◀── uses_config ── [JOB-001]
   │                                                                               │
   │                                                                               └──produces──▶ [RESULT-001]
   │
   └──has_revision──▶ [R20] ──...
```

위 그래프를 통해 AI는 다음과 같은 질의에 답할 수 있다:

- "HBM4E R30 FULLCHIP의 Signoff 현황은?" → Graph Traversal로 즉시 조회
- "최원우가 수행한 모든 Job의 결과는?" → Designer → SignoffJob → Result 경로 탐색
- "R20 대비 R30에서 새로 발생한 Fail 항목은?" → 두 Result 비교

---

## 3. Signoff Ontology 3-Layer 구조

### 3.1 Palantir 3-Layer 아키텍처 참조

Palantir는 현실 세계를 **3가지 계층**으로 모델링한다:

|Layer|Palantir 정의|핵심 질문|특징|
|---|---|---|---|
|**Semantic**|의미 계층|"무엇이 존재하는가?"|거의 변하지 않는 마스터 데이터|
|**Kinetic**|실행 계층|"어떻게 실행되는가?"|프로세스, 상태 변화 추적|
|**Dynamic**|의사결정 계층|"어떤 판단이 내려졌는가?"|결과, 의사결정, AI 학습 데이터|

**왜 3개 Layer로 나누는가?**

이 셋을 섞어버리면(예: RDB 테이블 하나에 다 넣으면), AI가 **"무엇이 원인(Semantic)"이고, "무엇이 결과(Dynamic)"이며, "내가 할 수 있는 행동(Kinetic)"이 무엇인지 구분하지 못한다.**


### 3.2 Signoff 도메인 3-Layer 매핑

```
┌─────────────────────────────────────────────────────────────────────┐
│  🎯 Dynamic Layer (의사결정 계층) - "The Brains"                    │
│  ─────────────────────────────────────────────────────────────────  │
│  "결과는 어떤가? 어떤 분석/판단이 필요한가?"                          │
│                                                                     │
│  [Phase 1] Result                                                   │
│  [Phase 2] CompareResult, CategorizePart, WaiverDecision            │
│  [Phase 3] SignoffIssue, AutoWaiverRecommendation                   │
│                                                                     │
│  → 실행의 결과물과 그에 대한 인간/AI의 판단                          │
│  → 피드백 루프를 형성하는 핵심 지식 데이터                           │
├─────────────────────────────────────────────────────────────────────┤
│  ⚡ Kinetic Layer (실행 계층) - "The Verbs"                         │
│  ─────────────────────────────────────────────────────────────────  │
│  "어떻게 실행되는가?"                                                │
│                                                                     │
│  SignoffJob, InputConfig, Workspace                                 │
│                                                                     │
│  → 시간의 흐름에 따라 발생하는 이벤트                                │
│  → 누가, 무엇을 가지고, 어떤 행위를 했는가?                          │
├─────────────────────────────────────────────────────────────────────┤
│  📚 Semantic Layer (자산 계층) - "The Nouns"                        │
│  ─────────────────────────────────────────────────────────────────  │
│  "무엇이 있는가?"                                                    │
│                                                                     │
│  Product, Revision, Block, Designer, SignoffApplication             │
│  [Phase 2] CriteriaSet                                              │
│                                                                     │
│  → 현실 세계의 물리적/논리적 자산과 환경 설정                        │
│  → Signoff 행위가 일어나기 전 이미 존재하는 불변/반고정 정보         │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.3 Layer별 역할과 Signoff 적용

#### ① Semantic Layer (자산): "AI가 건드려야 할 대상 정의"

- **Signoff 적용:** Product, Revision, Block, Designer, SignoffApplication
- **이유:** AI Agent에게 "이 Block이 검증 대상이야"라고 알려주는 기준이다. 이 Layer가 탄탄해야 나중에 툴이 바뀌거나(SPACE → PrimeSim), 공정이 바뀌어도(HBM3 → HBM4) 전체 구조가 무너지지 않는다.

#### ② Kinetic Layer (행위): "AI가 수행할 수 있는 Action 정의"

- **Signoff 적용:** SignoffJob, InputConfig, Workspace
- **이유:** 기존 DB는 '결과'만 저장하지만, Ontology는 **'과정'**을 저장한다. AI Agent가 "결과가 이상하네? **재실행(Action)** 해볼까?"라고 판단하려면, 실행이라는 행위 자체가 객체(SignoffJob)로 정의되어 있어야 한다.

#### ③ Dynamic Layer (지능): "AI의 판단 로직과 결과물"

- **Signoff 적용:** Result, (Phase 2) WaiverDecision, CompareResult
- **이유:** 단순한 Pass/Fail 데이터(Result)뿐만 아니라, **"왜 Waiver했는지(WaiverDecision)"**에 대한 인간의 판단 지식을 별도로 관리해야 한다. 그래야 나중에 AI가 "과거에 김책임님이 이런 패턴은 Waiver했으니, 이번에도 Waiver를 추천합니다"라고 **추론(Reasoning)**할 수 있다.


### 3.4 장기적인 이점 (Long-term Benefits)

이 구조로 가야만 얻을 수 있는 확실한 이점들이 있다.

**1. 확장성 (Scalability)**

- 새로운 Signoff Tool(예: 열 해석 툴)이 추가되어도 Semantic(Block)은 그대로 두고, Kinetic(새로운 Job 타입)과 Dynamic(새로운 결과 타입)만 붙이면 된다. 기존 구조를 엎을 필요가 없다.

**2. 설명 가능한 AI (Explainable AI)**

- AI가 어떤 결정을 내렸을 때, 역추적이 가능하다.
- _"Dynamic(이슈)이 발생한 원인은 Kinetic(어제 돌린 Job) 때문이고, 그 Job은 Semantic(변경된 Netlist)을 사용했다."_ 라는 식의 완벽한 문장형 설명이 가능해진다.

**3. Human-In-the-Loop에서 Out-of-the-Loop로의 진화**

- 처음에는 사람이 WaiverDecision(Dynamic)을 생성하지만, 데이터가 쌓이면 AI가 이 패턴을 학습하여 스스로 WaiverDecision 객체를 생성(제안)할 수 있게 된다.
- **구조가 같으므로 사람의 업무를 AI가 그대로 대체하기 쉽다.**


### 3.5 전체 관계도 (Phase 1)

```
                         ┌─────────────────────────────────────────────────────┐
                         │              Semantic Layer                         │
                         │         "무엇이 있는가?" (The Nouns)                 │
                         │                                                     │
                         │  ┌─────────┐    ┌──────────┐    ┌─────────┐        │
                         │  │ Product │───▶│ Revision │───▶│  Block  │        │
                         │  └─────────┘    └──────────┘    └────┬────┘        │
                         │                                      │             │
                         │  ┌──────────────────┐          ┌─────▼─────┐       │
                         │  │SignoffApplication│          │  Designer │       │
                         │  └────────┬─────────┘          └───────────┘       │
                         └───────────┼─────────────────────────────────────────┘
                                     │
                         ┌───────────┼─────────────────────────────────────────┐
                         │           ▼        Kinetic Layer                    │
                         │      "어떻게 실행되는가?" (The Verbs)                │
                         │                                                     │
                         │  ┌─────────────┐  uses   ┌─────────────┐            │
                         │  │ SignoffJob  │◀────────│ InputConfig │            │
                         │  └──────┬──────┘         └─────────────┘            │
                         │         │                                           │
                         │         │ executes_in    ┌───────────┐              │
                         │         └───────────────▶│ Workspace │              │
                         │                          └───────────┘              │
                         └───────────┬─────────────────────────────────────────┘
                                     │ produces
                         ┌───────────▼─────────────────────────────────────────┐
                         │               Dynamic Layer                         │
                         │     "어떤 결과와 판단이 있는가?" (The Brains)        │
                         │                                                     │
                         │  ┌────────────────┐                                 │
                         │  │     Result     │                                 │
                         │  └────────────────┘                                 │
                         │                                                     │
                         │  [Phase 2] CompareResult, WaiverDecision, ...       │
                         └─────────────────────────────────────────────────────┘
```


## 4. Phase별 구축 로드맵

### 4.1 Phase 분류 기준

| 기준     | Phase 1                    | Phase 2                     | Phase 3      |
| ------ | -------------------------- | --------------------------- | ------------ |
| **목표** | Ontology 기반 + Launcher 4.0 | ResultViewer 통합 + Dashboard | AI Agent 자동화 |
| **초점** | 실행 중심 (Job 생성/추적)          | 분석 중심 (Result/Waiver)       | 의사결정 자동화     |
| **기간** | 2026 H1                    | 2026 H2                     | 2027         |

### 4.2 Phase별 Object 배치

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Phase 1 (Core)                              │
│              Launcher 4.0 + Ontology 기반 구축                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Semantic:  Product, Revision, Block, Designer, SignoffApplication  │
│  Kinetic:   SignoffJob, InputConfig, Workspace                      │
│  Dynamic:   Result                                                  │
│                                                                     │
│  총 9개 Object - 핵심 실행 흐름 완성                                 │
│  ✓ Job 실행/추적  ✓ 입력 검증  ✓ 기본 결과 관리                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Phase 2 (Analysis)                          │
│              ResultViewer 통합 + Dashboard                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Semantic:  CriteriaSet (판정 기준)                                  │
│  Dynamic:   CompareResult, CategorizePart, WaiverDecision           │
│                                                                     │
│  +4개 Object 추가 - 분석/협업 기능 완성                              │
│  ✓ Revision 비교  ✓ Waiver 관리  ✓ 담당자 할당  ✓ Dashboard        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Phase 3 (Intelligence)                      │
│              AI Agent + Event-Driven Signoff                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Dynamic:   SignoffIssue (문의/해결 이력)                            │
│             AutoWaiverRecommendation (AI 추천)                       │
│             SignoffEvent (Event-Driven 트리거)                       │
│                                                                     │
│  +2~3개 Object 추가 - AI 자동화 기반 완성                            │
│  ✓ 자동 Waiver 추천  ✓ Event-Driven 실행  ✓ 이슈 자동 답변          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.3 Phase 1 구축 범위 상세

|영역|포함 내용|제외 내용 (Phase 2 이후)|
|---|---|---|
|**Object**|9개 핵심 Object|CriteriaSet, WaiverDecision 등|
|**기능**|Job 실행, 입력 관리, 결과 저장|비교, Waiver 관리, AI 추천|
|**연동**|Launcher 4.0|ResultViewer, Dashboard|
|**데이터**|PostgreSQL/Parquet|GraphRAG, Vector DB|

---


# Part 2: Phase 1 구현 상세

---

## 5. Phase 1 Object Type 상세 정의

### 5.1 Object Type 전체 목록 (9개)

| Layer        | Object Type        | 역할                       | 변경 빈도 |
| ------------ | ------------------ | ------------------------ | ----- |
| **Semantic** | Product            | 메모리 제품 (HBM4E, DDR5 등)   | 거의 없음 |
|              | Revision           | 설계 버전 (R00~R60)          | 낮음    |
|              | Block              | 회로 블록 (FULLCHIP, CORE 등) | 낮음    |
|              | Designer           | 설계자/담당자                  | 낮음    |
|              | SignoffApplication | 검증 도구 (19종)              | 거의 없음 |
| **Kinetic**  | SignoffJob         | 실행 이벤트 + 상태 추적           | 높음    |
|              | InputConfig        | 입력 설정 (재사용 가능)           | 중간    |
|              | Workspace          | 작업 공간 (Local/Central)    | 중간    |
| **Dynamic**  | Result             | 검증 결과 + 기본 통계            | 높음    |

---

### 5.2 Semantic Layer Objects (5개)

#### 5.2.1 Product (제품)

메모리 제품의 **최상위 개체**이다. HBM4E, DDR5, LPDDR5 등이 해당한다.

**역할**: Signoff 대상의 최상위 컨테이너. 모든 Revision, Block, Job은 궁극적으로 하나의 Product에 귀속된다.

**Properties:**

| 속성명                   | 타입        | 필수  | 설명            | 예시                                       |
| --------------------- | --------- | --- | ------------- | ---------------------------------------- |
| `product_id`          | STRING    | ✅   | 제품 고유 ID (PK) | `"HBM4E"`                                |
| `product_name`        | STRING    | ✅   | 제품 전체 이름      | `"HBM4E 32GB Wide I/O"`                  |
| `product_type`        | ENUM      | ✅   | 제품 종류         | `HBM`, `DRAM`, `FLASH`, `LPDDR`          |
| `technology_node`     | STRING    | ❌   | 공정 노드         | `"4nm"`, `"5nm"`                         |
| `status`              | ENUM      | ✅   | 개발 상태         | `PLANNING`, `ACTIVE`, `TAPED_OUT`, `EOL` |
| `tapeout_target_date` | DATE      | ❌   | Tapeout 목표일   | `"2026-06-30"`                           |
| `description`         | TEXT      | ❌   | 제품 설명         |                                          |
| `created_at`          | TIMESTAMP | ✅   | 생성 시각         |                                          |
| `updated_at`          | TIMESTAMP | ✅   | 수정 시각         |                                          |

**Links:**

| 관계명            | 방향  | 대상       | 카디널리티 | 설명         |
| -------------- | --- | -------- | ----- | ---------- |
| `has_revision` | →   | Revision | 1:N   | 제품의 설계 버전들 |
| `managed_by`   | →   | Designer | N:M   | 제품 담당 관리자들 |

---

#### 5.2.2 Revision (설계 버전)

설계 버전(R00~R60)을 나타낸다. **Signoff 수행의 기준 단위**이다.

**역할**: 동일 제품 내에서 설계 진행 상황을 구분하는 버전. 각 Revision은 고유한 Netlist 버전과 필수 Signoff 목록을 가진다.

**Properties:**

| 속성명                 | 타입            | 필수  | 설명                   | 예시                                                      |
| ------------------- | ------------- | --- | -------------------- | ------------------------------------------------------- |
| `revision_id`       | STRING        | ✅   | 리비전 고유 ID (PK)       | `"HBM4E_R30"`                                           |
| `product_id`        | STRING        | ✅   | 소속 제품 ID (FK)        | `"HBM4E"`                                               |
| `revision_code`     | ENUM          | ✅   | 리비전 코드               | `R00`~`R60`                                             |
| `design_stage`      | ENUM          | ✅   | 설계 단계                | `SCHEMATIC_ONLY`, `PRE_LAYOUT`, `POST_LAYOUT`           |
| `status`            | ENUM          | ✅   | 진행 상태                | `NOT_STARTED`, `IN_PROGRESS`, `COMPLETED`, `SIGNED_OFF` |
| `netlist_version`   | STRING        | ❌   | Netlist 버전           | `"v2.3.1"`                                              |
| `netlist_base_path` | STRING        | ❌   | Netlist 기준 경로        | `"/data/HBM4E/R30/netlist/"`                            |
| `required_apps`     | ARRAY[STRING] | ✅   | 필수 Signoff App ID 목록 | `["DSC", "LSC", "PEC"]`                                 |
| `release_date`      | DATE          | ❌   | 릴리즈 일자               |                                                         |
| `created_at`        | TIMESTAMP     | ✅   | 생성 시각                |                                                         |
| `updated_at`        | TIMESTAMP     | ✅   | 수정 시각                |                                                         |

**Links:**

| 관계명                 | 방향  | 대상       | 카디널리티 | 설명                  |
| ------------------- | --- | -------- | ----- | ------------------- |
| `of_product`        | →   | Product  | N:1   | 소속 제품               |
| `has_block`         | →   | Block    | 1:N   | 이 Revision의 Block들  |
| `previous_revision` | →   | Revision | N:1   | 이전 Revision (비교 기준) |

**Revision별 Signoff 수행 범위:**

| Revision | 설계 단계                  | 주요 Signoff                                   |
| -------- | ---------------------- | -------------------------------------------- |
| R00~R20  | Pre-Layout (Schematic) | Voltage Finder, PN Ratio, Floating Node, PEC |
| R30~R40  | Post-Layout (RC 반영)    | DSC, LSC, LS, Cana-TR                        |
| R50~R60  | Final (Waveform 기반)    | ADV Margin, Glitch, Dynamic DC Path          |

---

#### 5.2.3 Block (회로 블록)

회로 설계의 **계층적 블록**이다. FULLCHIP, CORE, PHY, IP 등이 해당한다.

**역할**: Signoff 수행의 대상 단위. 계층 구조를 가지며, 각 Block은 담당 설계자가 지정된다.

**Properties:**

| 속성명               | 타입        | 필수  | 설명                  | 예시                                        |
| ----------------- | --------- | --- | ------------------- | ----------------------------------------- |
| `block_id`        | STRING    | ✅   | 블록 고유 ID (PK)       | `"HBM4E_R30_FULLCHIP"`                    |
| `revision_id`     | STRING    | ✅   | 소속 Revision ID (FK) | `"HBM4E_R30"`                             |
| `block_name`      | STRING    | ✅   | 블록 이름               | `"FULLCHIP"`                              |
| `block_type`      | ENUM      | ✅   | 블록 유형               | `TOP`, `CORE`, `PHY`, `IO`, `IP`, `MACRO` |
| `hierarchy_path`  | STRING    | ✅   | 계층 경로               | `"/FULLCHIP/CORE/BL_DECODER"`             |
| `parent_block_id` | STRING    | ❌   | 상위 블록 ID (FK)       | `"HBM4E_R30_CORE"`                        |
| `description`     | TEXT      | ❌   | 블록 설명               |                                           |
| `updated_at`      | TIMESTAMP | ✅   | 수정 시각               |                                           |
| `instance_count`  | INTEGER   | ❌   | 인스턴스 수              | `128`                                     |
| `created_at`      | TIMESTAMP | ✅   | 생성 시각               |                                           |


**Links:**

| 관계명                    | 방향  | 대상       | 카디널리티 | 설명          |
| ---------------------- | :-: | -------- | :---: | ----------- |
| `of_revision`          |  →  | Revision |  N:1  | 소속 Revision |
| `parent_block`         |  →  | Block    |  N:1  | 상위 블록       |
| `child_blocks`         |  ←  | Block    |  1:N  | 하위 블록들      |
| `responsible_designer` |  →  | Designer |  N:1  | 담당 설계자      |

---

#### 5.2.4 Designer (담당자)

설계자, Signoff 수행자, Application 개발자 등 **모든 구성원**을 포함한다.

**역할**: Signoff 업무에 참여하는 모든 인력. Block 담당, Job 실행, Issue 보고/해결 등의 주체가 된다.

**Properties:**

| 속성명           | 타입        | 필수  | 설명             | 예시                                         |
| ------------- | --------- | :-: | -------------- | ------------------------------------------ |
| `designer_id` | STRING    |  ✅  | 담당자 고유 ID (PK) | `"kim_cs"`                                 |
| `name`        | STRING    |  ✅  | 이름             | `"김철수"`                                    |
| `email`       | STRING    |  ✅  | 이메일            | `"kim_cs@samsung.com"`                     |
| `employee_id` | STRING    |  ❌  | 사번             | `"A12345"`                                 |
| `team`        | STRING    |  ✅  | 소속 팀           | `"HBM Design Team"`                        |
| `role`        | ENUM      |  ✅  | 역할             | `DESIGNER`, `LEAD`, `DEVELOPER`, `MANAGER` |
| `is_active`   | BOOLEAN   |  ✅  | 활성 여부          | `true`                                     |
| `created_at`  | TIMESTAMP |  ✅  | 생성 시각          |                                            |
| `updated_at`  | TIMESTAMP |  ✅  | 수정 시각          |                                            |

**Links:**

| 관계명               | 방향  | 대상                 | 카디널리티 | 설명                 |
| ----------------- | :-: | ------------------ | :---: | ------------------ |
| `responsible_for` |  ←  | Block              |  1:N  | 담당 블록들             |
| `manages`         |  ←  | Product            |  N:M  | 관리하는 제품들           |
| `develops`        |  ←  | SignoffApplication |  N:M  | 개발 담당 Application들 |
| `executed_jobs`   |  ←  | SignoffJob         |  1:N  | 실행한 Job들           |
| `created_configs` |  ←  | InputConfig        |  1:N  | 생성한 InputConfig들   |

---

#### 5.2.5 SignoffApplication (검증 도구)

DSC, LSC, PEC 등 **19종의 Signoff Application**을 정의한다.

**역할**: Signoff 검증 도구의 메타데이터. 실행 방법, 지원 조건, 비교 기준 등을 정의한다.

**Properties:**

|속성명|타입|필수|설명|예시|
|---|---|:-:|---|---|
|`app_id`|STRING|✅|Application ID (PK)|`"DSC"`|
|`app_name`|STRING|✅|전체 이름|`"Driver Size Check"`|
|`app_group`|ENUM|✅|그룹 분류|`PRE_LAYOUT`, `STATIC`, `DYNAMIC`, `TIMING`|
|`engine_type`|ENUM|✅|실행 엔진|`SPACE`, `ADV`, `PRIMESIM`, `PRIMETIME`|
|`comparison_key`|STRING|✅|결과 비교 기준 컬럼|`"measure_net,driver_nmos"`|
|`supported_pvt_corners`|ARRAY[STRING]|✅|지원 PVT 조건들|`["SSPLVCT", "FFPHVHT"]`|
|`runscript_base_path`|STRING|✅|RUNSCRIPT 기준 경로|`"/RUNSCRIPTS/DSC/"`|
|`description`|TEXT|❌|Application 설명||
|`created_at`|TIMESTAMP|✅|생성 시각||
|`updated_at`|TIMESTAMP|✅|수정 시각||

**Links:**

|관계명|방향|대상|카디널리티|설명|
|---|:-:|---|:-:|---|
|`developed_by`|→|Designer|N:M|개발 담당자들|
|`used_by_configs`|←|InputConfig|1:N|이 App용 InputConfig들|

**Signoff Application 전체 목록 (19종):**

|ID|이름|그룹|엔진|comparison_key|
|---|---|---|---|---|
|DSC|Driver Size Check|STATIC|SPACE|measure_net,driver_nmos|
|LSC|Latch Setup Check|STATIC|SPACE|master,latch_name|
|LS|Level Shifter|STATIC|SPACE|master|
|CANATR|Coupling Analysis TR|STATIC|SPACE|victim_net,aggressor_net|
|CDA|Coupling Delay Analysis|TIMING|SPACE|victim_net,aggressor_net|
|PEC|Power/ESD Checker|PRE_LAYOUT|SPACE|unit_name,msg|
|PNRATIO|PN Ratio Checker|PRE_LAYOUT|PERC|inst_name,cell_name|
|FANOUT|Fan-Out Checker|PRE_LAYOUT|PERC|drv_net|
|DCPATH|DC Path Checker|STATIC|PRIMESIM|path_id,node|
|FLOATNODE|Floating Node Checker|PRE_LAYOUT|SPACE|node_name|
|ADV_MARGIN|ADV Margin Analyzer|DYNAMIC|ADV|name,fullmaster|
|DRIVER_KEEPER|Driver Keeper|DYNAMIC|ADV|instance_name,target_master|
|GLITCH|Glitch Margin Check|DYNAMIC|ADV|name,fullmaster|
|DYNAMIC_DC_PATH|Dynamic DC Path|DYNAMIC|SPACE|id,instance|
|CURRENT_ANALYZER|Current Analyzer|DYNAMIC|ADV|-|
|PT_DSC|PrimeTime DSC|TIMING|PRIMETIME|net,driver_full_master|
|PT_CANA|PrimeTime Cana|TIMING|PRIMETIME|victim_net,victim_driver|
|BA_DUMP_NETLIST|BA Dump Netlist|PRE_LAYOUT|PRIMESIM|-|
|VOLTAGE_FINDER|Voltage Power Finder|PRE_LAYOUT|PRIMESIM|top_net_name|

---

### 5.3 Kinetic Layer Objects (3개)

#### 5.3.1 SignoffJob (실행 이벤트)

실제 LSF에서 실행되는 **Signoff 작업 단위**이다.

**역할**: Signoff 실행의 핵심 이벤트. 실행 상태, 결과 연결 등 모든 실행 정보를 담는다. 동일 조건으로 재실행 시 새로운 Job이 생성된다.

**Properties:**

|속성명|타입|필수|설명|예시|
|---|---|:-:|---|---|
|**식별 정보**|||||
|`job_id`|STRING|✅|Job 고유 ID (PK)|`"JOB-20250119-143022-001"`|
|`config_id`|STRING|✅|사용 InputConfig ID (FK)|`"CFG-HBM4E-R30-DSC-001"`|
|`block_id`|STRING|✅|대상 Block ID (FK)|`"HBM4E_R30_FULLCHIP"`|
|`workspace_id`|STRING|✅|실행 Workspace ID (FK)|`"WS-LOCAL-kim_cs-001"`|
|`executed_by`|STRING|✅|실행자 Designer ID (FK)|`"kim_cs"`|
|**실행 상태**|||||
|`status`|ENUM|✅|실행 상태|`PENDING`, `RUNNING`, `DONE`, `FAILED`, `CANCELLED`|
|`lsf_job_id`|STRING|❌|LSF Job ID|`"12345678"`|
|`queue_name`|STRING|❌|LSF Queue 이름|`"normal"`|
|`submission_time`|TIMESTAMP|❌|제출 시각||
|`start_time`|TIMESTAMP|❌|시작 시각||
|`completion_time`|TIMESTAMP|❌|완료 시각||
|`runtime_seconds`|INTEGER|❌|실행 시간(초)|`43800`|
|**실행 환경**|||||
|`job_directory`|STRING|❌|실행 디렉토리 경로|`"/user/.../DSC_20250119/"`|
|`log_path`|STRING|❌|로그 파일 경로|`"/user/.../logs/pipeline.log"`|
|`error_message`|TEXT|❌|에러 메시지 (실패 시)||
|**이력 관리**|||||
|`previous_job_id`|STRING|❌|재실행 시 이전 Job ID|`"JOB-20250118-001"`|
|`rerun_reason`|STRING|❌|재실행 사유|`"Power 설정 오류 수정"`|
|`created_at`|TIMESTAMP|✅|생성 시각||
|`updated_at`|TIMESTAMP|✅|수정 시각||

**Links:**

|관계명|방향|대상|카디널리티|설명|
|---|:-:|---|:-:|---|
|`uses_config`|→|InputConfig|N:1|사용한 입력 설정|
|`targets_block`|→|Block|N:1|대상 Block|
|`executes_in`|→|Workspace|N:1|실행 Workspace|
|`executed_by`|→|Designer|N:1|실행자|
|`produces`|→|Result|1:1|생성된 결과|
|`previous_job`|→|SignoffJob|N:1|재실행 시 이전 Job|

**상태 전이 다이어그램:**

```
                    ┌────────────────────────────────────────┐
                    │                                        │
                    ▼                                        │
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    │
│ PENDING │───▶│ RUNNING │───▶│  DONE   │    │ FAILED  │    │
└─────────┘    └────┬────┘    └─────────┘    └────┬────┘    │
                    │                             │          │
                    │         ┌───────────┐       │          │
                    └────────▶│ CANCELLED │◀──────┘          │
                              └───────────┘                  │
                                    │                        │
                                    └─── (재실행) ────────────┘
```

---

#### 5.3.2 InputConfig (입력 설정)

재사용 가능한 **입력 설정 템플릿**이다.

**역할**: Signoff 실행에 필요한 입력 정보를 별도 관리. 동일 설정으로 여러 Job 실행 가능. 입력 검증 및 변경 추적의 핵심.

**왜 별도 Object로 분리하는가?**

|통합 시 문제점|분리 시 이점|
|---|---|
|동일 입력으로 여러 Job 실행 시 중복 저장|재사용으로 중복 제거|
|입력 변경 추적 어려움|버전 관리로 변경 이력 명확|
|템플릿 관리 불가|팀 내 표준 Config 공유 가능|
|Power 정의 오류 분석 불가|생성 시점에 검증 가능 (오류 50% 사전 방지)|

**Properties:**

|속성명|타입|필수|설명|예시|
|---|---|:-:|---|---|
|**식별 정보**|||||
|`config_id`|STRING|✅|설정 고유 ID (PK)|`"CFG-HBM4E-R30-DSC-001"`|
|`config_name`|STRING|✅|설정 이름|`"HBM4E R30 DSC Standard"`|
|`app_id`|STRING|✅|대상 Application (FK)|`"DSC"`|
|`revision_id`|STRING|✅|대상 Revision (FK)|`"HBM4E_R30"`|
|**입력 파일**|||||
|`netlist_path`|STRING|✅|Netlist 파일 경로|`"/data/HBM4E/R30/netlist.sp"`|
|`tech_file_path`|STRING|✅|Tech 파일 경로|`"/tech/4nm/tech.tf"`|
|**Power 정의**|||||
|`power_definition`|JSON|✅|Power 정의|`{"VDD": ["vdd_core"], "VSS": ["vss"]}`|
|`pvt_corner`|STRING|✅|PVT 조건|`"SSPLVCT"`|
|**시뮬레이션 파라미터**|||||
|`simulation_params`|JSON|❌|추가 파라미터|`{"threshold": 0.1}`|
|**검증 정보**|||||
|`is_validated`|BOOLEAN|✅|검증 완료 여부|`true`|
|`validation_result`|JSON|❌|검증 결과 상세|`{"power_check": "pass"}`|
|**버전/템플릿**|||||
|`version`|STRING|✅|버전|`"v1.2"`|
|`is_template`|BOOLEAN|✅|템플릿 여부|`false`|
|`parent_config_id`|STRING|❌|템플릿에서 파생 시 원본 ID|`"CFG-TEMPLATE-DSC"`|
|**메타데이터**|||||
|`created_by`|STRING|✅|생성자 (FK)|`"kim_cs"`|
|`created_at`|TIMESTAMP|✅|생성 시각||
|`updated_at`|TIMESTAMP|✅|수정 시각||

**Links:**

|관계명|방향|대상|카디널리티|설명|
|---|:-:|---|:-:|---|
|`for_application`|→|SignoffApplication|N:1|대상 Application|
|`for_revision`|→|Revision|N:1|대상 Revision|
|`created_by`|→|Designer|N:1|생성자|
|`used_by_jobs`|←|SignoffJob|1:N|이 Config를 사용한 Job들|
|`derived_from`|→|InputConfig|N:1|템플릿에서 파생된 경우|

---

#### 5.3.3 Workspace (작업 공간)

Signoff 작업이 실행되고 결과가 저장되는 **작업 공간**이다.

**역할**: Local Workspace(개인 실행 공간)와 Central Workspace(결과 공유 공간)를 통합 관리한다. Local에서 실행 후 Central로 동기화하는 흐름을 추적한다.

**Properties:**

|속성명|타입|필수|설명|예시|
|---|---|:-:|---|---|
|`workspace_id`|STRING|✅|Workspace 고유 ID (PK)|`"WS-LOCAL-kim_cs-001"`|
|`workspace_type`|ENUM|✅|타입|`LOCAL`, `CENTRAL`|
|`base_path`|STRING|✅|기본 경로|`"/user/HBM4E/VERIFY/SIGNOFF/..."`|
|`product_id`|STRING|❌|관련 Product ID|`"HBM4E"`|
|`owner_id`|STRING|❌|소유자 Designer ID (Local)|`"kim_cs"`|
|`storage_name`|STRING|❌|Storage 이름|`"hbm4e_storage"`|
|**동기화 정보**|||||
|`is_synced`|BOOLEAN|✅|Central 동기화 여부|`false`|
|`synced_to_workspace_id`|STRING|❌|동기화된 Central Workspace ID|`"WS-CENTRAL-HBM4E"`|
|`synced_at`|TIMESTAMP|❌|동기화 시각||
|**메타데이터**|||||
|`created_at`|TIMESTAMP|✅|생성 시각||
|`updated_at`|TIMESTAMP|✅|수정 시각||

**Links:**

|관계명|방향|대상|카디널리티|설명|
|---|:-:|---|:-:|---|
|`owned_by`|→|Designer|N:1|소유자 (Local인 경우)|
|`of_product`|→|Product|N:1|관련 제품 (Central인 경우)|
|`synced_to`|→|Workspace|N:1|동기화 대상 Central Workspace|
|`has_jobs`|←|SignoffJob|1:N|이 공간에서 실행된 Job들|
|`stores_results`|←|Result|1:N|저장된 Result들|

**Workspace 흐름:**

```
┌─────────────────────────────────────────────────────────────────────┐
│  Local Workspace (개인)              Central Workspace (공유)       │
│  /user/{PRODUCT}/VERIFY/...          /WORKSPACE/{PRODUCT}/...      │
│                                                                     │
│  ┌─────────────┐                     ┌─────────────┐               │
│  │ SignoffJob  │                     │   Result    │               │
│  │ 실행        │ ──── 동기화 ────▶   │   공유      │               │
│  │ result.csv  │                     │   분석      │               │
│  └─────────────┘                     └─────────────┘               │
│                                                                     │
│  • HPC 계정별 용량 제한               • 팀 전체 접근 가능           │
│  • 실행 중심                          • 결과 분석 중심              │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 5.4 Dynamic Layer Objects (1개 - Phase 1)

#### 5.4.1 Result (검증 결과)

SignoffJob에서 생성된 **검증 결과**이다.

**역할**: Signoff 실행의 산출물. Row 단위 통계(전체/Pass/Fail)를 관리하고, Central Workspace 업로드 상태를 추적한다.

> **Note**: Phase 1에서는 기본 통계만 관리. `waiver_count`, `waiver_progress_pct` 등 Waiver 관련 속성은 **Phase 2**에서 WaiverDecision 도입 시 추가.

**Properties:**

|속성명|타입|필수|설명|예시|
|---|---|:-:|---|---|
|**식별 정보**|||||
|`result_id`|STRING|✅|Result 고유 ID (PK)|`"RESULT-20250119-001"`|
|`job_id`|STRING|✅|생성 Job ID (FK)|`"JOB-20250119-143022-001"`|
|`workspace_id`|STRING|✅|저장 Workspace ID (FK)|`"WS-LOCAL-kim_cs-001"`|
|**결과 파일 정보**|||||
|`result_file_path`|STRING|✅|결과 파일 경로|`"/user/.../result.parquet"`|
|`result_format`|ENUM|✅|파일 형식|`CSV`, `PARQUET`|
|`file_size_bytes`|BIGINT|❌|파일 크기|`1048576`|
|**Row 통계**|||||
|`row_count`|INTEGER|✅|전체 Row 수|`1500`|
|`pass_count`|INTEGER|✅|Pass 수|`1200`|
|`fail_count`|INTEGER|✅|Fail 수|`300`|
|**Central 동기화**|||||
|`is_uploaded`|BOOLEAN|✅|Central 업로드 여부|`false`|
|`central_workspace_id`|STRING|❌|업로드된 Central Workspace ID||
|`uploaded_at`|TIMESTAMP|❌|업로드 시각||
|**메타데이터**|||||
|`created_at`|TIMESTAMP|✅|생성 시각||
|`updated_at`|TIMESTAMP|✅|수정 시각||

**Links:**

|관계명|방향|대상|카디널리티|설명|
|---|:-:|---|:-:|---|
|`produced_by`|→|SignoffJob|1:1|생성한 Job|
|`stored_in`|→|Workspace|N:1|저장 Workspace|
|`uploaded_to`|→|Workspace|N:1|업로드된 Central Workspace|

---

## 6. 관계(Links) 전체 정의

### 6.1 Phase 1 관계 전체 목록

|관계명|From|To|카디널리티|설명|Layer|
|---|---|---|:-:|---|---|
|**Semantic 내부**||||||
|`has_revision`|Product|Revision|1:N|제품의 설계 버전들|Semantic|
|`has_block`|Revision|Block|1:N|버전의 블록들|Semantic|
|`previous_revision`|Revision|Revision|N:1|이전 버전 (비교 기준)|Semantic|
|`responsible_designer`|Block|Designer|N:1|블록 담당자|Semantic|
|`managed_by`|Product|Designer|N:M|제품 관리자들|Semantic|
|`developed_by`|SignoffApplication|Designer|N:M|App 개발 담당자들|Semantic|
|**Kinetic 내부**||||||
|`uses_config`|SignoffJob|InputConfig|N:1|Job이 사용한 입력 설정|Kinetic|
|`executes_in`|SignoffJob|Workspace|N:1|Job 실행 Workspace|Kinetic|
|`executed_by`|SignoffJob|Designer|N:1|Job 실행자|Kinetic|
|`previous_job`|SignoffJob|SignoffJob|N:1|재실행 시 이전 Job|Kinetic|
|`synced_to`|Workspace|Workspace|N:1|Local→Central 동기화|Kinetic|
|`owned_by`|Workspace|Designer|N:1|Workspace 소유자|Kinetic|
|`derived_from`|InputConfig|InputConfig|N:1|템플릿에서 파생|Kinetic|
|**Cross-Layer (Semantic ↔ Kinetic)**||||||
|`targets_block`|SignoffJob|Block|N:1|Job 대상 Block|Sem↔Kin|
|`for_application`|InputConfig|SignoffApplication|N:1|설정 대상 App|Sem↔Kin|
|`for_revision`|InputConfig|Revision|N:1|설정 대상 Revision|Sem↔Kin|
|`created_by`|InputConfig|Designer|N:1|설정 생성자|Sem↔Kin|
|`of_product`|Workspace|Product|N:1|Workspace 관련 제품|Sem↔Kin|
|**Cross-Layer (Kinetic ↔ Dynamic)**||||||
|`produces`|SignoffJob|Result|1:1|Job이 생성한 결과|Kin↔Dyn|
|`stored_in`|Result|Workspace|N:1|결과 저장 Workspace|Kin↔Dyn|
|`uploaded_to`|Result|Workspace|N:1|Central 업로드 대상|Kin↔Dyn|

### 6.2 관계 다이어그램 (전체)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Semantic Layer                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────┐  has_revision   ┌──────────┐  has_block   ┌─────────┐        │
│   │ Product │────────────────▶│ Revision │─────────────▶│  Block  │        │
│   └────┬────┘      1:N        └─────┬────┘     1:N      └────┬────┘        │
│        │                            │                        │             │
│        │ managed_by            previous_revision        responsible        │
│        ▼                            │                    _designer         │
│   ┌──────────┐◀─────────────────────┘                        │             │
│   │ Designer │◀──────────────────────────────────────────────┘             │
│   └────┬─────┘                                                             │
│        │                                                                   │
│        │ developed_by        ┌──────────────────┐                          │
│        └────────────────────▶│SignoffApplication│                          │
│                              └────────┬─────────┘                          │
│                                       │                                    │
└───────────────────────────────────────┼────────────────────────────────────┘
                                        │ for_application
                                        ▼
┌───────────────────────────────────────┼────────────────────────────────────┐
│                              Kinetic Layer                                 │
├───────────────────────────────────────┼────────────────────────────────────┤
│                                       │                                    │
│   ┌─────────────┐  uses_config   ┌────┴────────┐                          │
│   │ SignoffJob  │◀───────────────│ InputConfig │                          │
│   └──────┬──────┘      N:1       └─────────────┘                          │
│          │                              ▲                                  │
│          │ executes_in                  │ for_revision                     │
│          │ executed_by                  │ created_by                       │
│          ▼                              │                                  │
│   ┌─────────────┐              ┌────────┴───┐                              │
│   │  Workspace  │◀── synced_to ──┤ Designer │ │ │ └──────┬──────┘ N:1 └────────────┘ │ │ │ │ └──────────┼─────────────────────────────────────────────────────────────────┘ │ produces ▼ ┌──────────┼─────────────────────────────────────────────────────────────────┐ │ │ Dynamic Layer │ ├──────────┼─────────────────────────────────────────────────────────────────┤ │ │ │ │ ┌──────▼──────┐ │ │ │ Result │──── stored_in ────▶ Workspace │ │ └─────────────┘ │ │ │ │ [Phase 2] CompareResult, WaiverDecision, CategorizePart │ │ │ └────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. ID 체계 및 생성 규칙

### 7.1 ID 설계 원칙

| 원칙 | 설명 |
|------|------|
| **고유성** | 전역적으로 유일해야 함 |
| **가독성** | 사람이 읽고 대략적 의미 파악 가능 |
| **정렬 가능** | 시간순 정렬이 가능하도록 타임스탬프 포함 |
| **추적 가능** | ID만 보고 소속(Product, Revision 등) 유추 가능 |

### 7.2 Object별 ID 규칙

| Object Type | ID 패턴 | 예시 |
|-------------|---------|------|
| **Product** | `{PRODUCT_CODE}` | `HBM4E`, `DDR5` |
| **Revision** | `{PRODUCT}_{REVISION_CODE}` | `HBM4E_R30` |
| **Block** | `{REVISION}_{BLOCK_NAME}` | `HBM4E_R30_FULLCHIP` |
| **Designer** | `{username}` | `kim_cs` |
| **SignoffApplication** | `{APP_CODE}` | `DSC`, `LSC` |
| **InputConfig** | `CFG-{PRODUCT}-{REV}-{APP}-{SEQ}` | `CFG-HBM4E-R30-DSC-001` |
| **Workspace** | `WS-{TYPE}-{OWNER/PRODUCT}-{SEQ}` | `WS-LOCAL-kim_cs-001`, `WS-CENTRAL-HBM4E-001` |
| **SignoffJob** | `JOB-{YYYYMMDD}-{HHMMSS}-{SEQ}` | `JOB-20250119-143022-001` |
| **Result** | `RESULT-{YYYYMMDD}-{SEQ}` | `RESULT-20250119-001` |

### 7.3 ID 생성 함수 (Python 예시)
```python
from datetime import datetime
import uuid

class IDGenerator:
    _seq_counters = {}
    
    @classmethod
    def generate_job_id(cls) -> str:
        """SignoffJob ID 생성"""
        now = datetime.now()
        date_str = now.strftime("%Y%m%d")
        time_str = now.strftime("%H%M%S")
        
        key = f"JOB-{date_str}"
        cls._seq_counters[key] = cls._seq_counters.get(key, 0) + 1
        seq = cls._seq_counters[key]
        
        return f"JOB-{date_str}-{time_str}-{seq:03d}"
    
    @classmethod
    def generate_config_id(cls, product: str, revision: str, app: str) -> str:
        """InputConfig ID 생성"""
        key = f"CFG-{product}-{revision}-{app}"
        cls._seq_counters[key] = cls._seq_counters.get(key, 0) + 1
        seq = cls._seq_counters[key]
        
        return f"{key}-{seq:03d}"
    
    @classmethod
    def generate_result_id(cls) -> str:
        """Result ID 생성"""
        now = datetime.now()
        date_str = now.strftime("%Y%m%d")
        
        key = f"RESULT-{date_str}"
        cls._seq_counters[key] = cls._seq_counters.get(key, 0) + 1
        seq = cls._seq_counters[key]
        
        return f"RESULT-{date_str}-{seq:03d}"
```

---

# Part 3: 활용 및 확장

---

## 8. Signoff Workflow와 Ontology 매핑

### 8.1 Workflow 단계별 Object 생성 시점

```
┌─────────────────────────────────────────────────────────────────────────────┐ │ Phase A: Initiation (준비 단계) │ │ ───────────────────────────────────────────────────────────────────────── │ │ │ │ • Criteria 합의 / 설계 환경 구성 │ │ • 제품 및 Revision 정의 │ │ • Block 구조 및 담당자 할당 │ │ • Signoff 대상 Application 선정 │ │ │ │ ▼ 생성되는 Object │ │ ┌─────────┐ ┌──────────┐ ┌─────────┐ ┌──────────┐ ┌──────────────────┐ │ │ │ Product │ │ Revision │ │ Block │ │ Designer │ │SignoffApplication│ │ │ └─────────┘ └──────────┘ └─────────┘ └──────────┘ └──────────────────┘ │ │ │ └─────────────────────────────────────────────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────────────────────────┐ │ Phase B: Execution (실행 단계) │ │ ───────────────────────────────────────────────────────────────────────── │ │ │ │ • Signoff Launcher에서 입력 설정 및 Job 제출 │ │ • HPC/LSF 기반 Signoff 수행 │ │ • 결과 파일 (result.csv) 생성 │ │ │ │ ▼ 생성되는 Object │ │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────┐ │ │ │ InputConfig │ │ Workspace │ │ SignoffJob │ │ Result │ │ │ └─────────────┘ └─────────────┘ └─────────────┘ └────────┘ │ │ │ └─────────────────────────────────────────────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────────────────────────┐ │ Phase C: Analysis (분석 단계) - Phase 2 이후 │ │ ───────────────────────────────────────────────────────────────────────── │ │ │ │ • 결과 분석 및 Waiver/Fixed 처리 │ │ • 이전 Revision 비교 및 Migration │ │ • 이슈 등록 및 해결 │ │ │ │ ▼ 생성되는 Object (Phase 2) │ │ ┌───────────────┐ ┌─────────────────┐ ┌─────────────────┐ │ │ │ CompareResult │ │ CategorizePart │ │ WaiverDecision │ │ │ └───────────────┘ └─────────────────┘ └─────────────────┘ │ │ │ └─────────────────────────────────────────────────────────────────────────────┘
```


### 8.2 시나리오: DSC Signoff 실행

**Step 1: 입력 설정 생성 (InputConfig)**
```json
{
  "config_id": "CFG-HBM4E-R30-DSC-001",
  "config_name": "HBM4E R30 DSC Standard",
  "app_id": "DSC",
  "revision_id": "HBM4E_R30",
  "netlist_path": "/data/HBM4E/R30/netlist.sp",
  "tech_file_path": "/tech/4nm/tech.tf",
  "power_definition": {
    "VDD": ["vdd_core", "vdd_io"],
    "VSS": ["vss_core", "vss_io"]
  },
  "pvt_corner": "SSPLVCT",
  "is_validated": true,
  "created_by": "kim_cs"
}
```

**Step 2: Job 생성 및 실행 (SignoffJob)**
```json
{
  "job_id": "JOB-20250119-143022-001",
  "config_id": "CFG-HBM4E-R30-DSC-001",
  "block_id": "HBM4E_R30_FULLCHIP",
  "workspace_id": "WS-LOCAL-kim_cs-001",
  "executed_by": "kim_cs",
  "status": "RUNNING",
  "lsf_job_id": "12345678",
  "submission_time": "2025-01-19T14:30:22Z"
}
```

**Step 3: 결과 생성 (Result)**
```json
{
  "result_id": "RESULT-20250119-001",
  "job_id": "JOB-20250119-143022-001",
  "workspace_id": "WS-LOCAL-kim_cs-001",
  "result_file_path": "/user/.../result.parquet",
  "result_format": "PARQUET",
  "row_count": 1500,
  "pass_count": 1200,
  "fail_count": 300,
  "is_uploaded": false
}
```


---

## 9. Phase 2~3 확장 계획

### 9.1 Phase 2 추가 Object (4개)

#### CriteriaSet (판정 기준) - Semantic Layer

| 속성 | 설명 |
|------|------|
| `criteria_id` | 기준 고유 ID |
| `app_id` | 적용 Application |
| `rules` | Pass/Fail 판정 규칙 (JSON) |
| `version` | 버전 |

#### CompareResult (비교 결과) - Dynamic Layer

| 속성 | 설명 |
|------|------|
| `compare_id` | 비교 고유 ID |
| `source_result_id` | 이전 Result ID |
| `target_result_id` | 현재 Result ID |
| `new_fail_count` | 새로 발생한 Fail 수 |
| `fixed_count` | 해결된 항목 수 |
| `waiver_migrated_count` | Waiver 이관된 수 |

#### CategorizePart (담당자 지정) - Dynamic Layer

| 속성 | 설명 |
|------|------|
| `categorize_id` | 지정 고유 ID |
| `result_id` | 대상 Result ID |
| `category_rule` | 분류 규칙 |
| `assigned_to` | 담당자 ID |
| `row_count` | 해당 Row 수 |

#### WaiverDecision (Waiver 판단) - Dynamic Layer

| 속성 | 설명 |
|------|------|
| `decision_id` | 의사결정 고유 ID |
| `result_id` | 대상 Result ID |
| `decision_type` | WAIVER / FIXED / PENDING |
| `decision_reason` | 판단 근거 |
| `is_migrated` | 이전 Revision에서 이관 여부 |
| `decided_by` | 결정자 ID |

### 9.2 Phase 3 추가 Object (2~3개)

#### SignoffIssue (문의/해결 이력)

- MLM Jira 연동
- 문의 유형별 분류
- 해결 방법 축적
- AI 자동 답변 학습 데이터

#### AutoWaiverRecommendation (AI 추천)

- 과거 Waiver 패턴 학습
- 자동 Waiver 후보 추천
- 신뢰도 점수
- 승인/거부 피드백 루프

#### SignoffEvent (Event-Driven 트리거)

- Netlist 변경 감지
- 자동 Signoff 트리거
- 영향 분석 기반 선택적 실행

---

## 10. 예시 시나리오

### 10.1 시나리오: 새 Revision Signoff 시작

**상황**: HBM4E R30이 릴리즈되어 Signoff를 시작해야 함

**Ontology 활용:**

1. **Revision 생성**
```json
   {
     "revision_id": "HBM4E_R30",
     "product_id": "HBM4E",
     "previous_revision": "HBM4E_R20",
     "required_apps": ["DSC", "LSC", "LS", "PEC", "CANATR"]
   }
```

2. **R20 InputConfig 재활용**
   - `previous_revision` 관계를 통해 R20 Config 조회
   - 템플릿으로 사용하여 R30 Config 생성
   - `netlist_path`만 R30으로 변경

3. **일괄 Job 생성**
   - `required_apps` 기반으로 Block × Application 조합 생성
   - 각 조합에 대해 SignoffJob 생성

### 10.2 시나리오: Power 정의 오류 추적 (Phase 1 핵심 가치)

**문제**: DSC 결과에 Garbage가 많이 발생

**기존 방식**: 
- 어떤 Power 설정을 썼는지 추적 불가
- 동일 실수 반복 가능

**Ontology 활용**:
1. `SignoffJob` → `uses_config` → `InputConfig` 조회
2. `InputConfig.power_definition` 확인
3. 다른 성공 Job의 Config와 비교
4. 차이점 식별 → 오류 원인 파악


```
Result[Garbage 많음] ← produces ← SignoffJob[JOB-001] → uses_config → InputConfig[CFG-001] .power_definition: {"VDD": ["vdd_core"]} ← VSS 누락!

vs

Result[정상] ← produces ← SignoffJob[JOB-002] → uses_config → InputConfig[CFG-002] .power_definition: {"VDD": ["vdd_core"], "VSS": ["vss"]}

```

### 10.3 시나리오: Waiver 이관 (Phase 2)

**상황**: R20에서 Waiver한 항목을 R30으로 이관

**Ontology 활용:**

1. **비교 수행**
```

Revision[R20] ← previous_revision ← Revision[R30]

CompareResult 생성:

- source_result_id: R20 DSC Result
- target_result_id: R30 DSC Result
- comparison_key: "measure_net,driver_nmos"

````

2. **동일 항목 식별**
   - R20 WaiverDecision 조회
   - comparison_key 기준 R30에서 매칭

3. **Waiver 이관**
```json
   {
     "decision_id": "WD-R30-001",
     "result_id": "RESULT-R30-001",
     "decision_type": "WAIVER",
     "is_migrated": true,
     "source_decision_id": "WD-R20-001",
     "decision_reason": "R20에서 승인된 Known Issue (Corner Case)"
   }
```

---
